spring:
  profiles:
    active: dev

packaged.jdbc.driver.classes: postgresql|org.postgresql.Driver,mysql|com.mysql.jdbc.Driver,sqlserver|com.microsoft.sqlserver.jdbc.SQLServerDriver

---

#--create user appuser with password '70ucan';
#--create database adhocclick;
#--grant all privileges on database adhocclick to appuser;
#--grant usage on schema public to appuser;
#--grant select on all tables in schema public to appuser;

spring:
  profiles: dev
  datasource:
      username: appuser
      password: 70ucan
      url: jdbc:postgresql://localhost/adhocclick
      driver_class_name: org.postgresql.Driver

server:
  port: 8443
  ssl:
    enabled: true
    key-alias: tomcat
    key-store: classpath:keystore.p12
    key-store-password: devonly
    key-store-type: PKCS12

---

spring:
  profiles: prod

---